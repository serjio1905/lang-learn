<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link

<% if (page == "/") {%> active<% } %>

          " href="<%= config.root %>/" id="begin">Home</a>
          <a class="nav-item nav-link

<% if (page == "/learn") {%> active<% } %>

          " href="<%= config.root %>/learn" id="begin">Start</a>
          <a class="nav-item nav-link

<% if (page == "/dictionary") {%> active<% } %>

          " href="<%= config.root %>/dictionary" id="dictionary">Dictionary</a>
          <a class="nav-item nav-link

<% if (page == "/auth") {%> active<% } %>

          " href="<%= config.root %>/auth" id="authorization">Enter</a>
        </div>
      </div>
</nav>
